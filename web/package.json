{
  "name": "band-bootstrap",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-mailchimp-subscribe": "^2.1.0",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.1.1",
    "rebass": "^3.0.1",
    "styled-components": "^4.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "git pull && yarn && yarn build && firebase deploy"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "prettier": {
    "trailingComma": "all",
    "semi": false,
    "singleQuote": true
  },
  "devDependencies": {
    "babel-plugin-macros": "^2.6.1"
  }
}
